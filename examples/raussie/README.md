# coolproject

GPU hillshade with Rust (wgpu) + Python. One command:

    coolproject

Open http://127.0.0.1:8765 to play with sliders.

To use your own DEM:

    coolproject --dem path/to/float32.npy

Linux note: install Vulkan loader (e.g., `sudo apt install mesa-vulkan-drivers`).
